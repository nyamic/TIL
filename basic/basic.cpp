// #2743 단어 길이 재기

/*
    길이 재는 함수 뭐지
    string.size() / string.length
*/

#include <iostream>
#include <string>
int main()
{
    using namespace std;
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    string s;
    cin >> s;

    cout << s.length();


    return 0;
}